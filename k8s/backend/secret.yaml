apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: terraform-generator
  labels:
    app.kubernetes.io/name: backend
    app.kubernetes.io/component: backend
    app.kubernetes.io/part-of: terraform-generator
type: Opaque
stringData:
  # IMPORTANT: Replace with your actual API key before applying.
  # For production, use External Secrets Operator or Sealed Secrets instead.
  OPENAI_API_KEY: "REPLACE_ME"
